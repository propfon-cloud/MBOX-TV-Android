<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Required because the TV source is normally a local HTTP URL such as 192.168.x.x. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
